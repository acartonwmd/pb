#ifndef USER_H
#define USER

#include <string>

class User;

class User {
    std::string name;
    std::string password;
    std::string username;

    bool user_login(std::string uame, std::string password) { }

    
};

#endif